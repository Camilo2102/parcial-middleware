type Query {
    findEmployeeById(id: ID): Employee
    findDepartmentById(id: ID): Department
}

type Employee{
    id: ID,
    name: String,
    lastName: String,
    role: String
}

type Department {
    departmentName: String,
    description: String,
    employees: [Employee]
}