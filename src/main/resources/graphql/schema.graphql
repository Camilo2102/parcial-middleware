type Query {
    findById(id: ID): Employees
}

type Employees{
    id: ID,
    name: String,
    lastName: String,
    role: String
}

type Departments {
    departmentName: String,
    description: String,
    employees: [Employees]
}